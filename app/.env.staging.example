# ── CitySend Customer App — STAGING environment ───────────────────────────────
# Used by Vercel preview/staging deployments (not committed with real values).
# Find credentials: Supabase Dashboard → citysend-staging → Project Settings → API

VITE_SUPABASE_URL=https://your-staging-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-staging-anon-key

# Use Stripe TEST keys for staging (never live keys)
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...
